    precision highp float; uniform vec2 u_resolution; uniform float u_time; uniform vec2 u_mouse; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 mouse_uv = (u_mouse * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     float m_dist = length(uv - mouse_uv);     uv -= (uv - mouse_uv) * smoothstep(0.8, 0.0, m_dist) * 0.3;     vec2 uv0 = uv;     float t = u_time * 0.49855;     uv *= rot(t * 0.57586);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 10; i++) {         float fi = float(i);         uv = abs(uv) - vec2(0.65074, 1.25201); uv *= rot(sin(t + length(uv) * 2.12314) + t * 1.51350); uv.y += 0.08635 * cos(uv.x * 7.28253 - t);          float d = length(uv) * exp(-length(uv0) * 1.66770);         vec3 col = palette(length(uv0) + fi*0.87138 + t, vec3(0.74189, 0.28692, 0.11657), vec3(0.96683, 0.04709, 0.18997), vec3(2.19781, 2.98503, 1.78764), vec3(0.17292, 0.10103, 0.61976));         d = sin(d*5.15190 + t * 0.05108)/3.15367;         d = abs(d); d = pow(0.00552 / d, 1.24035);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }