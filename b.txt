[
  {
    "title": "F L E S H",
    "fsSource": "\n            precision highp float; uniform vec2 u_resolution; uniform float u_time; uniform vec2 u_mouse;\n            mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); }\n            vec2 hash( vec2 p ) { p = vec2( dot(p,vec2(127.1,311.7)), dot(p,vec2(269.5,183.3)) ); return -1.0 + 2.0*fract(sin(p)*43758.5453123); }\n            float noise( in vec2 p ) {\n                const float K1 = 0.366025404; const float K2 = 0.211324865; vec2 i = floor( p + (p.x+p.y)*K1 ); vec2 a = p - i + (i.x+i.y)*K2;\n                vec2 o = (a.x>a.y) ? vec2(1.0,0.0) : vec2(0.0,1.0); vec2 b = a - o + K2; vec2 c = a - 1.0 + 2.0*K2;\n                vec3 h = max( 0.5-vec3(dot(a,a), dot(b,b), dot(c,c) ), 0.0 );\n                vec3 n = h*h*h*h*vec3( dot(a,hash(i+0.0)), dot(b,hash(i+o)), dot(c,hash(i+1.0))); return dot( n, vec3(70.0) );\n            }\n            void main() {\n                vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y); float t = u_time * 0.5;\n                \n            vec2 mouse_uv = (u_mouse * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);\n            float m_dist = length(uv - mouse_uv);\n            uv -= (uv - mouse_uv) * smoothstep(0.8, 0.0, m_dist) * 0.3;\n        \n                uv *= 1.0 + 0.1 * sin(t * 3.0 + length(uv) * 4.0); uv *= rot(noise(uv * 2.0 + t) * 0.4);\n                vec2 z = uv; float iter = 0.0;\n                for(int i = 0; i < 40; i++) {\n                    z = vec2(z.x*z.x - z.y*z.y, 2.0*z.x*z.y) + vec2(0.35 + 0.1*sin(t), 0.35 + 0.1*cos(t*1.2));\n                    z += 0.15 * vec2(noise(z*2.0 + t), noise(z*2.0 - t)); if(length(z) > 4.0) break; iter++;\n                }\n                float smooth_iter = iter - log2(max(1.0, log2(dot(z,z)))) + 4.0; float flesh = sin(smooth_iter * 0.3 + t * 2.0) * 0.5 + 0.5;\n                float vein = sin(smooth_iter * 0.8 - t * 4.0) * 0.5 + 0.5; vec3 col = mix(vec3(0.15, 0.0, 0.05), vec3(0.7, 0.1, 0.2), flesh); \n                col = mix(col, vec3(0.3, 0.6, 0.1), vein * 0.3); col *= smoothstep(0.05, 0.3, length(uv) + 0.1*noise(uv*10.0+t*5.0));\n                gl_FragColor = vec4(col, 1.0);\n            }"
  },
  {
    "title": "G L I T C H G E O M",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 0.89163;     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 11; i++) {         float fi = float(i);         uv = uv / dot(uv, uv) - vec2(0.72405); uv *= rot(fi * 1.01870 + t * 0.66828); uv.y += 0.02496 * cos(uv.x * 4.46037 - t);          float d = length(uv) * exp(-length(uv0) * 1.21883);         vec3 col = palette(length(uv0) + fi*0.98500 + t, vec3(0.64226, 0.04892, 0.69627), vec3(0.05275, 0.01895, 0.80382), vec3(2.88492, 1.59619, 1.99356), vec3(0.69254, 0.48783, 0.15254));         d = sin(d*7.64454 + t * 0.98064)/4.07354;         d = abs(d); d = pow(0.00668 / d, 1.30584);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "V O I D D R I V E",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 0.44652;     uv *= rot(t * 0.55260);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 19; i++) {         float fi = float(i);         uv *= 1.0 + 0.01172 * sin(t * 0.02840 + length(uv) * 3.55584); uv *= rot(fi * 1.59376 + fi * 1.59376); uv.x += 0.38742 * sin(uv.y * 4.81166 + t); uv.y += 0.22928 * cos(uv.x * 7.63159 - t);          float d = length(uv) * exp(-length(uv0) * 1.96949);         vec3 col = palette(length(uv0) + fi*0.81723 + t, vec3(0.30870, 0.29209, 0.95592), vec3(0.76513, 0.44015, 0.48388), vec3(1.73325, 2.71569, 1.42254), vec3(0.99642, 0.95156, 0.85078));         d = sin(d*7.38940 + t * 1.30839)/8.95690;         d = abs(d); d = pow(0.01360 / d, 1.40354);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "P A R A S P A S M",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 2.54791;     uv *= rot(t * 0.11383);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 5; i++) {         float fi = float(i);         uv = uv / dot(uv, uv) - vec2(0.98947); uv *= rot(t * 0.67820 + length(uv0) * 6.57225); uv.y += 0.29495 * cos(uv.x * 9.46430 - t);          float d = length(uv) * exp(-length(uv0) * 1.65577);         vec3 col = palette(length(uv0) + fi*0.01927 + t, vec3(0.26443, 0.98573, 0.16982), vec3(0.59323, 0.36242, 0.00205), vec3(2.38695, 1.40595, 1.08755), vec3(0.33142, 0.96900, 0.38955));         d = sin(d*4.66024 + t * 4.42583)/2.37677;         d = abs(d); d = pow(0.01290 / d, 1.36045);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "P A R A C O R E",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 1.29038;     uv *= rot(t * 0.16583);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 6; i++) {         float fi = float(i);         uv *= 1.0 + 0.13658 * sin(t * 1.64013 + length(uv) * 5.94541); uv *= rot(sin(t + length(uv) * 4.21157) + length(uv0) * 4.73814); uv.y += 0.37831 * cos(uv.x * 5.36384 - t);          float d = length(uv) * exp(-length(uv0) * 0.53387);         vec3 col = palette(length(uv0) + fi*0.31367 + t, vec3(0.28741, 0.01060, 0.76848), vec3(0.38054, 0.30345, 0.97653), vec3(1.35724, 1.17384, 2.41097), vec3(0.45131, 0.60661, 0.17748));         d = sin(d*2.03600 + t * 4.95768)/2.20046;         d = abs(d); d = pow(0.01558 / d, 1.42791);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "G L I T C H S P A S M",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 0.46854;     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 13; i++) {         float fi = float(i);         uv = uv / dot(uv, uv) - vec2(0.96734); uv *= rot(t * 0.56648 + sin(t + length(uv) * 4.30305)); uv.x += 0.00821 * sin(uv.y * 12.19098 + t);          float d = length(uv) * exp(-length(uv0) * 0.97368);         vec3 col = palette(length(uv0) + fi*0.19015 + t, vec3(0.96844, 0.78765, 0.10935), vec3(0.05671, 0.17711, 0.54288), vec3(2.84514, 2.60139, 1.21106), vec3(0.67014, 0.38101, 0.49303));         d = sin(d*9.37867 + t * 4.79489)/8.16461;         d = abs(d); d = pow(0.00684 / d, 1.45188);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "C Y B E R M O R P H 57",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 0.28616;     uv *= rot(t * 0.52665);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 6; i++) {         float fi = float(i);         uv = abs(uv) - vec2(0.23459, 1.49246); uv *= rot(length(uv0) * 5.16031 + fi * 1.77553); uv.x += 0.20820 * sin(uv.y * 13.18766 + t); uv.y += 0.42568 * cos(uv.x * 4.94296 - t);          float d = length(uv) * exp(-length(uv0) * 1.44357);         vec3 col = palette(length(uv0) + fi*0.81866 + t, vec3(0.52314, 0.02706, 0.89587), vec3(0.37977, 0.15826, 0.99082), vec3(1.85324, 2.62043, 2.54623), vec3(0.54034, 0.39253, 0.71222));         d = sin(d*3.39805 + t * 0.51608)/3.33062;         d = abs(d); d = pow(0.02052 / d, 1.33587);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "X E N O C O R E",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 1.33081;     uv *= rot(t * 0.64913);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 15; i++) {         float fi = float(i);         uv *= 1.0 + 0.17730 * sin(t * 4.91617 + length(uv) * 2.16892); uv *= rot(fi * 2.99925 + fi * 2.99925); uv.x += 0.00675 * sin(uv.y * 4.41510 + t); uv.y += 0.12298 * cos(uv.x * 11.52652 - t);          float d = length(uv) * exp(-length(uv0) * 0.77595);         vec3 col = palette(length(uv0) + fi*0.32881 + t, vec3(0.11005, 0.91954, 0.26582), vec3(0.27876, 0.62216, 0.13422), vec3(2.73437, 1.57943, 1.30692), vec3(0.98547, 0.22010, 0.95268));         d = sin(d*3.24845 + t * 3.15321)/2.84127;         d = abs(d); d = pow(0.01414 / d, 1.37211);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  },
  {
    "title": "E C H O P U L S E",
    "fsSource": "precision highp float; uniform vec2 u_resolution; uniform float u_time; vec3 palette( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) { return a + b*cos( 6.28318*(c*t+d) ); } mat2 rot(float a) { float s = sin(a), c = cos(a); return mat2(c, -s, s, c); } void main() {     vec2 uv = (gl_FragCoord.xy * 2.0 - u_resolution.xy) / min(u_resolution.x, u_resolution.y);     vec2 uv0 = uv;     float t = u_time * 1.14961;     uv *= rot(t * 0.04006);     vec3 finalColor = vec3(0.0);     for(int i = 0; i < 6; i++) {         float fi = float(i);         uv = uv / dot(uv, uv) - vec2(0.41425); uv *= rot(length(uv0) * 1.99855 + sin(t + length(uv) * 0.96128)); uv.y += 0.37327 * cos(uv.x * 12.52945 - t);          float d = length(uv) * exp(-length(uv0) * 1.09522);         vec3 col = palette(length(uv0) + fi*0.27273 + t, vec3(0.73713, 0.18077, 0.12324), vec3(0.83550, 0.36177, 0.22355), vec3(2.41160, 1.47308, 1.43158), vec3(0.82556, 0.86530, 0.73754));         d = sin(d*5.20648 + t * 3.46884)/3.28310;         d = abs(d); d = pow(0.01531 / d, 1.17460);         finalColor += col * d;     }     gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0); }"
  }
]